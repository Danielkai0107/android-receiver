# 命令行構建指南（可選）

## ⚠️ 注意

**首次使用強烈建議用 Android Studio**，它會自動處理所有初始化。

如果您堅持使用命令行，請按以下步驟操作：

---

## 方法 1: 讓 Android Studio 初始化 Gradle（推薦）

```bash
# 1. 打開 Android Studio
./使用_Android_Studio.sh

# 2. 等待 Gradle 同步完成

# 3. 關閉 Android Studio

# 4. 現在可以用命令行了
./gradlew assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

---

## 方法 2: 手動安裝 Gradle（不推薦）

### 安裝 Gradle

```bash
# 使用 Homebrew 安裝
brew install gradle

# 驗證安裝
gradle --version
```

### 初始化 Gradle Wrapper

```bash
cd /Users/danielkai/Desktop/safe-net-app/android-receiver

# 生成 wrapper
gradle wrapper --gradle-version 8.2

# 驗證
./gradlew --version
```

### 構建專案

```bash
# 清理
./gradlew clean

# 構建 Debug APK
./gradlew assembleDebug

# 安裝到設備
adb install -r app/build/outputs/apk/debug/app-debug.apk

# 啟動 App
adb shell am start -n com.safenet.receiver/.presentation.main.MainActivity
```

---

## 方法 3: 直接使用現有的 Android SDK（適合進階用戶）

如果您已經安裝了 Android SDK：

```bash
# 設置環境變數
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools

# 使用 Android SDK 的 Gradle
cd /Users/danielkai/Desktop/safe-net-app/android-receiver
$ANDROID_HOME/gradle/gradle-8.2/bin/gradle wrapper
./gradlew assembleDebug
```

---

## 🎯 最簡單的方法

**真心建議**：

```bash
# 執行這個腳本
./使用_Android_Studio.sh

# Android Studio 會打開
# 等待 5-10 分鐘同步完成
# 點擊 Run 按鈕
# 完成！
```

**之後**所有命令行工具都可以正常使用了。

---

## 📊 對比

| 方法 | 時間 | 難度 | 推薦度 |
|------|------|------|--------|
| Android Studio | 10 分鐘 | ⭐ | ⭐⭐⭐⭐⭐ |
| Homebrew Gradle | 15 分鐘 | ⭐⭐⭐ | ⭐⭐⭐ |
| 手動配置 | 30+ 分鐘 | ⭐⭐⭐⭐⭐ | ⭐ |

---

## 💡 為什麼推薦 Android Studio？

1. **自動處理所有配置**
   - Gradle Wrapper
   - Android SDK
   - 依賴下載
   - 環境變數

2. **更可靠**
   - 經過測試的配置
   - 自動解決依賴衝突
   - 錯誤提示清晰

3. **開發體驗好**
   - 代碼補全
   - 即時錯誤檢查
   - 可視化調試
   - Logcat 查看

4. **之後可用命令行**
   - 初始化完成後
   - 所有命令行工具都可用
   - 兩全其美

---

## 🚀 立即開始

```bash
# 最快的方式
./使用_Android_Studio.sh

# 或
open -a "Android Studio" /Users/danielkai/Desktop/safe-net-app/android-receiver
```

**10 分鐘後，您就能在手機上看到 App 運行了！** 🎉
