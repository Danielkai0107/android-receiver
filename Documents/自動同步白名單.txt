✅ 已設定：自動同步白名單

功能更新：
━━━━━━━━━━━━━━━━━━━━━━━━━
🔄 每次打開 App 時自動同步白名單
🔄 每次返回主畫面時自動同步白名單
🚫 防止重複同步（如果正在同步中）

使用方式：
━━━━━━━━━━━━━━━━━━━━━━━━━
1. 打開 App → 自動同步 ✅
2. 切換到其他 App 再返回 → 自動同步 ✅
3. 不需要手動點「同步白名單」按鈕

顯示提示：
━━━━━━━━━━━━━━━━━━━━━━━━━
如果白名單為空：
  「Gateway 未註冊，將掃描所有 Beacon」

如果白名單不為空：
  「已同步 5 個白名單設備」

優點：
━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 自動化：無需手動操作
✅ 即時更新：總是獲取最新白名單
✅ 用戶友好：開啟即用
✅ 防止重複：避免同時多次同步

測試步驟：
━━━━━━━━━━━━━━━━━━━━━━━━━
1. 在 Android Studio 中 Run
2. App 打開時會自動同步白名單
3. 觀察主畫面顯示：
   - 白名單設備: 0 或更多
   - 最後同步時間: 剛剛
   - 提示訊息
4. 按 Home 鍵，再打開 App
5. 會再次自動同步 ✅

🎉 不需要再手動點同步按鈕了！
